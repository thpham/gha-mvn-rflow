<?xml version="1.0" encoding="UTF-8"?>
<layers xmlns="http://www.springframework.org/schema/boot/layers"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.springframework.org/schema/boot/layers
                            https://www.springframework.org/schema/boot/layers/layers-3.3.xsd">
  <!--
    Custom layer configuration for multi-module Maven project.
    Separates internal modules from external dependencies for optimal layer caching.
  -->
  <application>
    <into layer="spring-boot-loader">
      <include>org/springframework/boot/loader/**</include>
    </into>
    <into layer="application"/>
  </application>
  <dependencies>
    <!-- Internal project modules go into their own layer (checked FIRST) -->
    <into layer="internal-dependencies">
      <include>com.example.myproject:*</include>
    </into>
    <!-- External SNAPSHOT dependencies (after internal modules are matched) -->
    <into layer="snapshot-dependencies">
      <include>*:*:*SNAPSHOT</include>
    </into>
    <!-- Everything else is external dependencies -->
    <into layer="dependencies"/>
  </dependencies>
  <layerOrder>
    <layer>dependencies</layer>
    <layer>spring-boot-loader</layer>
    <layer>snapshot-dependencies</layer>
    <layer>internal-dependencies</layer>
    <layer>application</layer>
  </layerOrder>
</layers>
